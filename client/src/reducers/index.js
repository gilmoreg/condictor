const initialState = {
  ticketList: [],
  openTickets: [],
  user: null,
};

export default function rootReducer(state = initialState, action) {
  switch (action.type) {
    default: return state;
  }
}
